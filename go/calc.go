package calc

// Double returns n * 2. The starter function — every ticket on the Go
// stack adds a sibling here.
func Double(n int) int {
	return n * 2
}

// Triple returns n * 3.
func Triple(n int) int {
	return n * 3
}
